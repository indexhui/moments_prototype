# 展覽版本比較

- 展覽預設入口 `/game/exhibition` 顯示「展覽改善版」。
- `/game/exhibition/tgs` 顯示「東京電玩展版本」，比較基準是 commit `437cc17` 的展覽流程。
- 展覽頁左側區塊可在兩個版本間切換。切換時沿用目前網址的章節、語言等參數；左側的章節跳轉與錄影場景選擇保留目前版本。

兩個入口目前共用展覽流程元件與素材，透過 `ExhibitionEdition` 區分版本。後續只屬於展覽改善版的劇情、玩法或素材調整，需依版本分支處理或建立版本專用檔案，避免東京電玩展版本跟著改變。
